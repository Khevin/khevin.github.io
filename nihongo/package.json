{
  "name": "nihongo-tooling",
  "version": "1.0.0",
  "private": true,
  "description": "Local-only build/process scripts for the nihongo app. Not a published package — exists so node_modules has a home and scripts can require their dependencies cleanly.",
  "type": "module",
  "scripts": {
    "process-particles": "node scripts/process-particle-blobs.mjs",
    "characterize": "node tests/capture.mjs tests/golden-baseline.json",
    "validate-data": "node scripts/validate-data.mjs",
    "build-vendor": "node scripts/build-vendor.mjs",
    "verify": "node scripts/validate-data.mjs && node tests/capture.mjs tests/golden-after.json && node tests/compare.mjs tests/golden-baseline.json tests/golden-after.json",
    "test:interactions": "node tests/interactions.mjs"
  },
  "dependencies": {
    "sharp": "^0.33.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "playwright": "^1.60.0",
    "ts-fsrs": "^5.4.1"
  }
}
