.claude/
.design-ref/
.design-ref-hero/
.DS_Store
Thumbs.db

# Node deps + lock files for the nihongo image-optimization scripts.
nihongo/node_modules/
# nihongo/package-lock.json is committed (pinned deps for the reader extension).

# Scratch + WIP artefacts.
*.tmp
*.tmp.js
*.webp.tmp

# Large reference PDFs the user keeps in nihongo/ for offline lookup.
# Source code only in git; reference books live in the working tree.
nihongo/*.pdf

# Editor noise.
*.swp
.idea/
.vscode/

# Generated unpacked extension bundle (load locally; not deployed via Pages).
nihongo/reader-extension/dist/

# Generated sample library package for the reader extension (rebuild with `npm run build:sample-library`).
nihongo/reader-extension/fixtures/sample-library.zip

# KANJIDIC2 download cache for scripts/build-kanji-index.mjs (the generated kanji-index.json is committed)
nihongo/.cache/

# Separate private repo, cloned inside this one by mistake-proofing; never publish it here.
lastro-playbook/
